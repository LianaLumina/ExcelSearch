# 进度快照（暂停点 · 2026-09）

> 用途：用户要求"做完即暂停并关机"。本文件是**恢复工作时的入口**，不依赖对话记忆。
> 恢复后先读本文 → 再读 `docs/回迁标注.md`（回迁总清单，21 条）→ 再按用户指令开始 V0.3.2 回迁。

---

## 1. 当前位置

- 仓库：`<prototype>`（git，无远端）；**HEAD 见 `git log -1`**；工作区干净。
- 阶段：**V0.3.1.5 Qt 原型已完成**（功能 + 控件动效 + 性能冒烟），**V0.3.2 回迁尚未开始**（等用户指令）。
- 原型已交付能力：搜索 / 二级筛选（标准·逐级）/ 屏蔽 / 标记 / 标记筛选 / 缓存 / 可配置智能列 /
  搜索历史 / 共享模式 / 托盘 / 关闭方式选择 / 高级设置门禁 / 自检钩子体系 / **两轮控件动效**（可一键关闭）。

## 2. 本轮（暂停前）完成的四件事

1. **确认明文自动删除不迁移** ✅ —— 并且**代码级验证**：原型 `main.cpp` 内
   `DeleteFileW / fs::remove / remove_all / SHFileOperation / QFile::remove` **全部 0 命中**
   （仅有的两处 `std::remove_if` 是内存向量过滤），原型从代码上不具备删除文件的能力。
2. **性能与健壮性冒烟** ✅（详见 `docs/性能与健壮性冒烟报告.md`，脚本 `tools\smoke.ps1` 可复跑）
   - 健壮性：11 个文件的数据集（0 字节 / 随机字节 / `PK` 头半坏 zip / 坏 xse·docx·xls / 空 csv /
     只有表头 / 10 万行 CSV）**全部不崩、不挂起**；
   - **抓到一个真 bug 并修掉**：模糊补充的去重是 `O(n×m)` 双重循环，10 万命中下**单次搜索 75 秒** →
     改为 O(n+m) + 精确命中 ≥5000 时跳过模糊补充 → **2,423 ms（31×）**；
   - 顺手清理：删除从未调用的死函数 `columnSourceExists()`、修掉 1 处 `-Wmisleading-indentation`；
     **现在 `-Wall -Wextra` 下 main.cpp 零警告**（已实测）。
3. **废弃代码：原型侧已清** ✅ —— 加严编译（`-Wunused-function/variable`）**无任何 unused 报告**；
   手工核查出并删除了唯一一处死函数。
4. **废弃代码：原版侧清单已核实到 file:line** ✅ —— `docs/回迁标注.md` **第 15.1 节**
   列出 5 组要删的功能及其精确符号/行号/配置键（修改窗口标题 / 应用图标 / 界面透明度 / 毛玻璃 / 统计报表），
   并标注了 `UpdateWindowTitle()` **身兼两职**（自定义标题 + `[共享模式]` 后缀）这个删除陷阱。

## 3. 未修项（已记录，留给回迁阶段决定）

见冒烟报告第 4 节：**A** 加载失败文件无反馈 / **B** 离线模式缺数据目录时文案说"共享目录不可达" /
**C** 大数据量下缓存收益变小 / **E** 超大结果集填表慢。四条都不影响当前可用性。

## 4. 下一步（**已暂停，等用户**）

- **回迁尚未开始**。用户 2026-09 明确："先别开始，我还有事要安排"。
- **暂停原因（当前）**：等用户装完 **7-Zip**（用于回迁后的还原点归档）后再继续。
  ⚠️ 归档需要 **7-Zip**（Windows 自带的 `tar.exe` / `Compress-Archive` 只能出 .zip，生不成 .7z）。
  详见 `docs/prototype-archive/migration-cleanup-plan.md`。
- **本轮已拍板的三项决定**：
  1. **迁移落点**：新建 `<repo>`（拷 `V0.3.0` 作底 + 换掉 Win32 UI）。
     理由：原程序 `<workspace>` **没有任何版本控制**（.git 不存在），新建目录 = 天然还原点。
  2. **配置后端**（回迁清单第 9 条的前置决策，**已解决**）：**迁到 `config.ini`**，
     并写一次性「注册表 `HKCU\Software\ExcelSearch` → ini」搬迁脚本（否则老用户升级后个性化设置全丢）。
  3. **回迁后清理与归档**（**有硬前提，未执行**）：见 `docs/回迁后清理与归档计划.md`。
- 用户下令开始后，迁移起手式：
  1. 读 `docs/回迁标注.md`（21 条，含"回迁自检建议"14 条）；
  2. 动效整段搬运按 **第 19 → 20** 条；删除废弃代码按 **第 15.1** 节；
  3. 每步跑 `tools\selfcheck.ps1 -NoAnim`（离屏 13 张截图 + 业务字段）与 `tools\smoke.ps1`。

## 5. 边界与卫生（恢复后仍适用）

- 不要 `git push`（无远端）；不要 rebase / amend 既有提交（本轮我修正过一次**自己刚产生的**错误提交，
  原因见下）；不要删 `docs/` 下文档。
- **绝对不要跑 `--clearmarks`**（会清掉用户配置里的 7 条屏蔽 + 1 条标记）。
- 用户当前配置：`dark=true`、`accent=#f59e0b`（橙）、`closeAction=ask`、`data\` **5 个文件**（其中 1 个 `.xse`）
  → 基线 `files=5 entries=43846 skipped=0`、搜索「工日」`hits=54 blocked=7`。**别拿旧的蓝色/6 文件基线比对。**
- 本轮卫生：`<smoke-dir>`（冒烟隔离数据集，约 7MB）与 `<prototype>\build_warn`（警告扫描构建）
  已删除/可删；`.gitignore` 已补 `build*/` 规则，防止 `build_warn` 这类目录再被误提交
  （本轮曾误提交 30 个构建产物，已用 `--amend` 修正为只含 `.gitignore` + `main.cpp`）。
