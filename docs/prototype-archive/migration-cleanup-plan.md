# 回迁后清理与归档计划（**尚未执行**）

> 来源：用户 2026-09 决定 —— 原文：
> "新建一个 V0.3.2，但**正式完成迁移与所有验证全部通过后**，将原有的 Qt 测试版和原 V0.3.0 删除，
> 仅保留一份，不然这个项目里面的文件太多了，顺便把以前所有的还原点版本全部都用压缩文件（保存为 .7z）形式保存"。
>
> ⚠️ **这是有前提的破坏性操作。未满足全部前提前，任何人都不要执行。**

---

## 1. 硬前提（三条全满足才可动手）

1. **V0.3.2 回迁正式完成**（Qt UI 已替换原 Win32 UI，构建通过）；
2. **回迁清单全部验证通过**：`docs/回迁标注.md` 的 21 条 + **14 条回迁自检建议**逐条过，
   且 `tools\selfcheck.ps1 -NoAnim` 与 `tools\smoke.ps1` 的基线全部对上；
3. **用户明确下令执行清理**（本文件只是预案，不是执行许可）。

## 2. 要做的三件事（顺序不能反）

1. **先归档**（见第 4 节）——**归档成功并抽验可解压之后**，才进入删除；
2. 删除 **Qt 测试版原型** `<prototype>`（含 `build\`、56 张预览图、`.git`）；
3. 删除 **原 `<repo-v0.3.0>`**。
   → 最终只保留 `<repo>` 一份。

**删除前的安全动作**：把待删目录清单（含规模）先列给用户确认一次；确认后先归档、再删。

## 3. 工具现状

**7-Zip 已安装**，位置未必在默认目录：
```
<7-Zip 安装目录>\7z.exe
```
- 注册表卸载项显示 `InstallLocation = <7-Zip 安装目录>`（这也是最终确认它的依据）；
  标准路径 `C:\Program Files (x86)\7-Zip`、`%LOCALAPPDATA%\Programs\7-Zip`、PATH、scoop/choco 均无。
- **→ 任何脚本都必须写全路径**（PATH 里没有 `7z`），不要依赖 `7z` 短命令。
- 若将来换机器：先用「注册表卸载项 + 开始菜单快捷方式」找安装位置，比猜路径可靠。

## 4. 归档范围（**待用户确认哪些算"还原点版本"**）

`<workspace>` 下实测规模：

| 目录 | 规模 | 是否"还原点版本"？ |
|---|---|---|
| `V0.3.0_还原点` | 29.5 MB / 268 文件 | ✅ 明确算 |
| `v0.2.0_交接包` | 8.1 MB / 153 文件 | ❓ 待确认（是交接包，且更旧） |
| `linux_交接包` | 1.5 MB / 64 文件 | ❓ 待确认 |
| `<Linux 版目录>` | 1.5 MB / 63 文件 | ❓ 待确认（开发中目录） |
| `<加密工具目录>` | 7.4 MB / 142 文件 | ❓ 待确认（开发中目录） |
| `V0.3.0` | 45.7 MB / 418 文件 | 删除对象（第 2 节），不归档 |
| `qt_ui_proto`（`<workspace>\` 下） | 27.4 MB / 379 文件 | 删除对象（第 2 节），不归档 |

- 归档命名建议：`<目录名>_归档_YYYYMMDD.7z`，放在**同级或专门的归档目录**（位置待定）。
- 归档后建议**抽验**：`7z t <包>` 校验完整性 + 随机解一个文件确认可读，再删原目录。

## 5. 执行记录

| 日期 | 步骤 | 结果 | 产物 |
|---|---|---|---|
| 2026-09-18 | **归档还原点/交接包**（用户："先按照我前面说的做完"） | ✅ 3 个包全部打包 + `7z t` 校验通过；**原目录未删** | `<archive>\` 下 3 个 `.7z` |

归档明细（合计 **39.1 MB → 7.07 MB**）：

| 原目录 | 压缩包 | 原始 | 压缩后 | 校验 |
|---|---|---|---|---|
| `V0.3.0_还原点` | `V0.3.0_还原点_归档_20260918.7z` | 29.5 MB | 3.82 MB | ✅ |
| `v0.2.0_交接包` | `v0.2.0_交接包_归档_20260918.7z` | 8.1 MB | 2.81 MB | ✅ |
| `linux_交接包` | `linux_交接包_归档_20260918.7z` | 1.5 MB | 0.44 MB | ✅ |

**尚未处理（等用户拍板）**：
- 这 3 个**原目录是否删除**（删掉才真正达到"项目文件变少"的目的；包已校验，可随时解压恢复）；
- `<Linux 版目录>`（1.5 MB）与 `<加密工具目录>`（7.4 MB，**09-07 仍在改动，疑似活跃目录**）**是否也归档**；
- `V0.3.0` 与 `qt_ui_proto` 的删除仍**严格按第 1 节前提**执行（回迁完成 + 全部验证通过 + 用户下令）——**目前回迁已暂停**。
