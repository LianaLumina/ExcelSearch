# 动效验收清单 / 验收记录（主会话）

> **状态：动效已完工并复核通过（2026-09）**。本文档原为"接续用清单"，现转为**验收记录**。
> 权威交接记录：`docs/动效交接.md`（动效专班 → 主会话）；设计与节拍表：`docs/动效设计.md`；
> 回迁注意：`docs/回迁标注.md` 第 **16 / 19 / 20** 条。
> 动效期间提交：`7076300`（起点）→ `7468ada`（实现结束）→ `5c17f96`（交接单）。

---

## 0. 验收结论（主会话独立复核，非采信对方结论）

| 复核项 | 结果 |
|---|---|
| 构建 | `ninja: no work to do`（已最新）/ 无 error |
| **业务字段** | `--report`（关闭动效）逐字段与基线一致，**新增 `anim=off`**；开启那轮为 `anim=on` → **开关确实生效** |
| 搜索 / 筛选 / 列名 | `hits=54 blocked=7`；`辅助=26 检修=28`（标准模式）；`核定工日 → OK` |
| **不变量 A：开启(落定) vs 关闭** | **13 张截图 0 差异**（`tools\selfcheck.ps1` 一轮跑出） |
| **不变量 B：关闭 vs 动效前** | 独立重建预动效构建（`7076300` 独立工作树）对比：**10/13 逐字节相同**，另 3 张差异**已逐一定位并证明非回归**（见第 2 节） |
| 结构 | `CMakeLists.txt` 未动（`git diff 7076300..HEAD -- CMakeLists.txt` 为空）；共享 core 近 3 天无改动；单实例闸门仍在 |
| 配置 / 构建格式 | 动效**零新增配置键、零新增依赖/Qt 模块** |

**结论：动效层的实现可信、可一键关闭、关闭后与动效前一致。可以进入真机手验（第 3 节）。**

---

## 1. 必查项（复核时逐条过）

| # | 检查项 | 判据 | 本次结果 |
|---|---|---|---|
| 1 | 构建通过 | `cmake --build build` 无错误 | ✅ |
| 2 | **GUI 子系统未被破坏** | PE 可选头 `Subsystem == 2`；双击无黑框 | ✅（见第 17 条） |
| 3 | **单实例闸门仍在** | 第二实例立刻退出、进程数恒为 1 | ✅ |
| 4 | **关闭动效开关存在且有效** | `UI_PROTO_NO_ANIM=1` 或 `--no-anim`；`--report` 的 `anim=` 随之变化 | ✅ |
| 5 | **业务字段逐字段一致** | 见下方基线 | ✅ |
| 6 | 搜索未变 | `--search 工日` → `hits=54 blocked=7` | ✅ |
| 7 | 二级筛选未变 | 标准模式 `辅助→检修` = `26→28`；`检修→辅助` = `28→26` | ✅ |
| 8 | 缓存未变 | 清缓存 → `miss` → 再跑 `hit` | ✅（交接单已跑，本次未重复） |
| 9 | **未改共享 core** | 无 V0.3.0 下改动 | ✅（按修改时间核，近 3 天无写入） |
| 10 | 未引入新依赖 / QML | `CMakeLists.txt` 无新增 `find_package` | ✅ |

**基线（2026-09 当前环境；★注意环境已变，见下）**
```
files=5  entries=43846  skipped=0  cache=hit  blockedEntries=7  blockedFiles=0  marked=1
hist=0  histShow=5  histTtl=0  filterMode=standard  shareMode=off  closeAction=ask
tray=off(离屏)  anim=off|on
搜索「工日」→ hits=54 blocked=7
```
- **`skipped` 由 1 变 0**：`data\` 现在只剩 **5 个文件**（`.xlsx`×3 + `.docx`×1 + `.xse`×1）。
  被删掉的是 `105大修工日审查电气（初版）中核检修.xse` —— 它**正是原来那条被附加密码跳过的文件**；
  留下的 `regression-multisheet.xse` 是正常文件。所以 `files=5 / entries=43846` 不变、`skipped` 1→0。
- **强调色不再是蓝色**：当前 `accent=#f59e0b`（橙），是**用户自己在真机上改的**（动效实例从不写配置）。
  基线截图里的颜色随用户设置变化，**不要硬编码 `#326cf3` 去比对**。
- 用户配置里另有：7 条屏蔽 + 1 条标记 + `dark=true` + `closeAction=ask`（**不要用 `--clearmarks`**）。

---

## 2. 视觉验收（主会话实测证据）

工具：`tools\selfcheck.ps1`（离屏 + `Start-Process -Wait` + 13 张截图矩阵，`-NoAnim` 关闭动效）。

**不变量 A —— 动效开启(落定) vs 关闭：13 张 0 差异**（同环境，脚本一次跑出）✅

**不变量 B —— 关闭动效 vs 动效前**：旧基线 PNG 因环境变化（橙强调色 / 少一个文件）已失效，
故用**独立工作树重建预动效构建**（`git worktree add <worktree> 7076300`）在同一环境重拍 13 张对比，
结果 **10 张逐字节相同**，3 张有差异且**逐一定位证明非回归**：

| 差异图 | 差异区域 | 定位证据 | 判定 |
|---|---|---|---|
| `search-light` / `search-dark` / `empty-light` | 卡片标题栏右侧 | 裁掉 `x>=860` 后**逐字节一致** | **有意新增**：折叠箭头（第 19.4 条已记录的唯一例外） |
| `sec4-light` / `sec4-dark`（共享设置） | 底部状态行 | 裁掉 `y>=320` 后**逐字节一致** | **测试方法造成**：基线构建位于 `_anim_base`（且无 `data\`）→ 状态行文本不同，与动效无关 |

> 复核方法留档：`Crop()` 裁切后用 `Get-FileHash` 比对；整图比对作为对照（确实不一致）以排除"裁到无关区域"的假阳性。

**仍需真机手验的盲区**（离屏截不到 / 只能人手判断，见交接单第 7 节）：
- **真实鼠标 hover 态**（离屏无鼠标，`--hover` 是控件级兜底）；
- hover 跟手度、切页是否闪/抖、折叠手感。

---

## 3. 手验（交给用户，主会话无法代劳）

1. hover 跟手不卡、无抖动（按钮 / 设置列表项 / 结果表行）；
2. 顶部 Tab 与设置分区切换过渡自然、**不闪不抖**；
3. 折叠面板展开/收起顺滑（搜索页两张卡片）；⭐ 顺带确认**折叠状态不持久化**（重启即展开，这是有意的，第 19.2 条）；
4. 深色 + 换强调色后，动效颜色跟着变（当前强调色是**橙**）；
5. 托盘图标、托盘菜单、关闭方式对话框在动效下正常；
6. ⭐ 点「重新加载」→ **统计数字保持不变**（不闪 0）+ 顶部细条扫过（这两条是用户反馈后修的，见交接单第 7 节）。

若发现问题：把**现象 + 截图**发回，不必新开动效轮次。

---

## 4. 文档更新（本次已完成的收尾）

1. `docs/回迁标注.md`：第 **16 条**已把控件动效改为"✅ 已完成（原型内，两轮）"并指向 19/20；
   **第 19 条**（第一轮基线与开关 + 9 条实现要点/典型症状）、**第 20 条**（第二轮：新增项 / 七个踩过的坑 /
   零配置零构建变化 / 验收实测）；自检建议补到第 14 项。**（动效实例写的，主会话已复核对得上）**
2. `README.md`：特性列表、`--report` 新字段、自检钩子、截图参数、一键回归脚本已补齐。
3. `docs/动效设计.md`：新增（简报 / 曲线库与时长档位 / 19 条节拍表 / 组件契约 / 关闭开关 / 验证矩阵 /
   第二轮清单 / 刻意不做 / 已知取舍）。
4. **`docs/动效任务提示词.md`：已归档**（顶部标注"已完成，仅作历史记录"）。
5. **本文件：已更新为验收记录**（基线修正 + 复核证据 + 手验项）。
6. 仓库根 `proto_anim*.png`：动效实例留下的预览/证据图（`.gitignore` 忽略、不入库），保留备查。

---

## 5. 之后回到主线：回迁准备（V0.3.2）

动效已收口。`docs/回迁标注.md` 现有 **20 条**，回迁前**仍未完成**的：

- **【前置决策】OCR 工具链**（第 16 条）：`ocr_reader.cpp` 用 C++/WinRT，CMake 仅在 `if(MSVC)` 下加 cppwinrt；
  现原型是 MinGW → **必须先定工具链**（Qt 换 MSVC / 单独编 DLL / 换识别方案）。
- **【前置决策】配置后端**（第 9 条）：注册表 `HKCU\Software\ExcelSearch` ↔ `config.ini` 的取舍与搬迁脚本
  （**动效零新增配置键**，所以搬迁清单不变）。
- **后台废弃代码删除**（第 15 条）：修改窗口标题 / 应用图标 / 界面透明度 / 毛玻璃 / 统计报表
  —— 连同全局变量与配置键一起删（`g_customTitle`、`g_customIconPath`、`g_alphaPercent`、`g_blurPercent` 等）。
- **明文自动删除不迁移**（第 13 条）：确认不搬回。
- **性能与健壮性冒烟**：坏文件 / 错误密码 / 空数据 / 大数据量（尚未系统做）。
- 回迁后自检：`grep -n "★回迁注意" main.cpp` 逐条过 + `docs/回迁标注.md` 的"回迁自检建议"（现有 14 条）。

---

## 6. 边界（不要踩）

- 不要 `git push`（无远端）；不要 rebase / amend 既有提交；不要删 `docs/` 下文档。
- 不要动 `<repo-v0.3.0>`、`CMakeLists.txt` 的 `WIN32_EXECUTABLE TRUE` 与 `Qt6::Network`、单实例闸门。
- **绝对不要跑 `--clearmarks`**（会清掉用户配置里的 7 条屏蔽 + 1 条标记）。
  需要动配置的测试：先 `Copy-Item config.ini config.ini.bak`，测完还原。
- `build\bin\data` 不要动（现状 **5 个文件**，其中 1 个 `.xse`）。
- 复核用的独立工作树 `<worktree>` 用完请 `git worktree remove` 清理。
