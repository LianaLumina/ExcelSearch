# 更新日志

## V0.3.2

> 本版把界面从 **Win32 原生**整体重写为 **Qt 6**，业务核心（检索/解析/加密）沿用并做了性能加固。
> 下面是相对 V0.3.0 的完整改动。

### 一、界面：Win32 → Qt 6 全面重写

- **无边框自绘窗口**（MAA 风格视觉语言）：深/浅双主题、5 色强调色（含自定义取色）、圆角面板；
- **控件动效**（可一键关闭）：hover 颜色过渡、页面/分区切换淡入位移、可折叠卡片、数据概览数值滚动、
  结果表入场错峰、加载进度细条、密码错误抖动；
- **关闭方式可选**：点 × 弹「关闭方式」对话框（最小化到托盘 / 直接关闭，支持"不再询问"），
  并可在「设置 → 通用设置」里固定为三选一；
- **托盘**：左键/双击恢复；右键菜单「显示主界面 / 退出程序」；无托盘环境自动降级；
- 程序图标与资源管理器一致（exe 内嵌图标同时用于任务栏与托盘）；
- **使用说明书窗口**（版式与交互参照 MAA 的「公告」框）：**独立顶层窗口**（非模态，可与主界面并用，
  标题区可拖动、右上角只有**最大化/还原**按钮——与 MAA 原版公告窗 `ShowCloseButton=False`
  `ShowMinButton=False` 的设置一致，关闭只能走「确认」）；左侧章节导航（标题精简，无横向滚动条）
  + 右侧 Markdown 正文（Qt 原生渲染，强制按窗口宽度换行，无横向滚动条）+ 左下装饰位（默认留白，预留图片接口）
  + 右下「☐ 下次更新前不再展示」与「确认」居中成组（读完前该复选不可勾选，与 MAA 相同）；
  **启动时内容有更新才弹出**，勾「下次更新前不再展示」后不再打扰，内容更新后自动恢复提示；
  未读完点「确认」依次出现调侃文案（连续点 20 次以上放行），Alt+F4/Esc 同样受该门禁约束；
  入口在「关于我们」页；「通用设置」另有永久开关。

### 二、功能

| 能力 | 说明 |
|---|---|
| **搜索** | 精确搜索；**模糊搜索**作为补充（精确命中很多时自动跳过，见性能一节）；正则（`re:` 前缀） |
| **二级筛选（两种模式）** | **标准**：改条件＝换条件，每次从基准重算（与顺序无关）；**逐级**：显式追加条件（单调收窄） |
| **屏蔽 / 标记** | 文件级 + 条目级屏蔽；5 色标记；`已标记颜色` / `已标记红` 等直接检索 |
| **可配置智能列** | 结果表 3–7 列（`序号/文件名/匹配内容` 固定），中间列可映射到任意表头列名 |
| **搜索历史** | 存储 20 条；下拉显示条数可设；时效 5 档（10 分钟 ~ 12 小时 / 关闭程序后删除） |
| **共享模式** | UNC 共享目录作为数据源；不可达时自动吃缓存且**不覆盖**缓存；共享缓存 TTL 30 小时（离线 10 天） |
| **缓存** | `cache.dat`（索引）+ `cache.inv`（全量清单，含被跳过的加密文件），清单逐项精确匹配才复用 |
| **加密表格** | `.xse` 解密；「附加密码」加密的表首次加载提示输入 |
| **首次运行引导** | 数据目录为空时明确提示"把文件放进该目录后点重新加载"，并显示完整路径 |
| **加载失败汇总** | 损坏/不支持的文件不再静默丢弃，加载后汇总提示；区分"读取失败 / 无有效数据 / 需附加密码" |

支持格式：`.xlsx` / `.xls` / `.csv` / `.docx` / `.xse`

### 三、迁移与兼容（从 V0.3.0 升级）

- 配置后端由**注册表** `HKCU\Software\ExcelSearch` 改为 **`%APPDATA%\ExcelSearch\config.ini`**；
- **首次启动自动搬迁一次**：管理密码 / 深色主题 / 屏蔽（文件级+条目级）/ 标记 / 搜索历史 / 共享模式与路径；
  - 注册表**只读、原样保留**（可回退旧版）；
  - **废弃项不迁**：自定义标题 / 应用图标 / 窗口透明度 / 毛玻璃；原版死键 `DataPath` 也不迁；
  - 原版两级历史中只迁「一级搜索历史」（本版不记录二级筛选历史）；
  - 可手动重跑：`excel_search.exe --migrate --out result.txt`

### 四、构建与交付

- **构建**：CMake ≥ 3.20 + Ninja + MSYS2 MinGW-w64 + Qt6（Widgets、Network）；产物为 **GUI 子系统**可执行文件；
- **部署** `tools\deploy.ps1`：`windeployqt` + **依赖闭包补齐**（MSYS2 的 windeployqt 不带 MinGW 运行时与
  Qt 传递依赖，实测会缺 20+ 个 DLL）+ 开源许可收录 + 空 `data\` + **递归依赖校验** + **无 Qt 环境启动测试**；
- **安装包** `tools\build-installer.ps1`（Inno Setup 6，带自动重试）：
  - 管理员安装；**安装位置 / 桌面 / 开始菜单 / 任务栏**四项可选；
  - `data\` **空着自带**并**授予普通用户写权限**（装完丢文件即可用）；
  - 安装时展示 **GPL-3.0** 许可协议；
  - 卸载器命名为 **`uninstall.exe`**；
  - 卸载为**两页向导**：「卸载选项」（删除全部数据 / 删 data / 删用户配置，勾"全部"联动置灰）
    → 「确认卸载」（列出将删除/将保留的内容）→ 完成后明确提示哪些已删、哪些已保留；
  - 卸载前**自动结束正在运行的程序**（否则被占用的 exe/DLL 删不掉，会留下数十 MB 残骸）；
  - 静默卸载（`/VERYSILENT`）不弹界面且**绝不删用户数据**。

### 五、性能与健壮性

- **修复严重性能问题**：模糊补充的去重原为 `O(n×m)` 双重循环，10 万命中下单次搜索耗时 **75 秒**；
  改为 `O(n+m)` 哈希去重 + 精确命中 ≥5000 时跳过模糊补充 → **2.4 秒（约 31 倍）**；
- **畸形数据不崩**：0 字节 / 随机字节 / 半坏 zip / 各类坏扩展名 / 空 CSV / 10 万行大文件均不崩不挂起；
- 启动不再弹出黑色命令行窗口（GUI 子系统）；
- **单实例**：多实例会同时读写同一份配置与缓存，可能出现"清单来自 A、数据已被 B 覆盖"的**撕裂缓存**并
  静默给出错误结果 —— 现强制运行时唯一，重复启动时唤起已运行窗口后自行退出。

### 六、移除 / 不再包含

- **移除**旧后台功能：自定义窗口标题、应用图标、窗口透明度（滑块）、毛玻璃/丙烯酸、统计报表；
- **不再包含**旧版"明文文件自动删除"（该行为风险高，本版不做任何文件删除）；
- 程序以**普通用户权限**运行（不再要求管理员），无 UAC 提示。

### 七、许可证与致谢

- 本项目以 **GPL-3.0** 发布（见 `LICENSE`）；
- 第三方组件许可与声明见 `licenses/`（Qt 以 LGPL-3.0 动态链接使用，附全文与替换说明）；
- 界面**视觉风格**与**说明书弹窗的交互方式**（读到底才能关、未读完点确认被拦下、
  「下次更新前不再展示」的记忆语义）参考 MAA / MaaWpfGui 与 MaaEnd；本项目**未使用、未复制其任何
  源代码、样式表、XAML 或资源文件**（实现均为本项目自有的 C++/Qt 代码），核查过程见 `docs/LICENSE-COMPLIANCE.md`。
