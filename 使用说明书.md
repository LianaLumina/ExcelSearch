### 这是什么

**Excel 表格关键字搜索工具**帮你在**成百上千份表格**里，一眼找到**哪一份、哪一页、哪一行**出现了你要查的内容。

- 支持 Excel（`.xlsx` / `.xls`）、CSV、Word（`.docx`）以及加密表格（`.xse`）；
- 一次搜**全部文件**，不用一个个打开；
- 可以把常用的筛选、屏蔽、标记保存下来，下次直接看。

---

### 快速开始

**四步就能用起来：**

1. 打开程序目录下的 **`data`** 文件夹，把你的表格文件**复制进去**；
2. 启动程序（第一次启动会自动读取）；
3. 在搜索框输入关键词，按 **回车** 或点「**搜索**」；
4. 结果会列在下方，双击某一行可以**看到完整内容**。

> 已经放好文件但没显示？点一下工具栏的「**重新加载**」即可。

---

### 搜索的三种方式

- **按原样找**（最常用）：直接输入，例如 `工日`；
- **允许有点差异**：默认开启，会自动补上"字词相近"的结果，不用额外操作；
- **按规则找**：关键词前加 `re:`，例如 `re:^2024-\d+`。

小技巧：

- 想找**两个词同时出现**的单元格，就把它们连在一起输入（例如 `检修工日`）；
- 结果太多时，用下面的「缩小范围」继续筛。

---

### 缩小范围（二级筛选）

第一次搜索后，可以在「**二级筛选**」里继续输入关键词把结果筛小。有两种模式，可在
「**设置 → 搜索设置**」里切换：

- **标准模式（默认）**：每次筛选都从**第一次的结果**里重新找，**换一个词就是换一个条件**，
  结果和输入顺序无关。适合"我换个词再看看"。
- **逐级模式**：在**当前结果里继续筛**，越筛越少。适合"我要一层层收窄"。

两种模式随时可切换，也不会影响你的原始数据。

---

### 看懂结果列表

- 列表里的 **文件名 / 工作表 / 行号** 告诉你这一行在哪里；
- **双击一行** → 打开详情，可以看到该行的完整内容；
- **右键**：
  - 「**打开文件所在位置**」→ 直接跳到该文件；
  - 「**复制该行**」→ 复制内容到剪贴板；
  - 「**导出所选条目**」→ 导出成 Excel 文件；
- 上方「**数据概览**」显示：已加载文件数、索引记录数、本次命中数。

---

### 屏蔽与标记

**屏蔽**：某些行你不想再看到（例如作废的行）

- 在结果里**右键 → 屏蔽该行**；也可以屏蔽**整个文件**；
- 屏蔽后，之后的搜索都不会再出现它们。

**标记**：重要的行想留着以后找

- 在结果里**右键 → 标记**，可选 **红 / 紫 / 蓝 / 绿 / 黄** 五种颜色；
- 想看标记过的内容，直接在搜索框输入 **`已标记颜色`**（或 `已标记红` 等）；
- 取消：右键 → 取消标记；全部清理在「**设置 → 高级设置 → 标记清除**」。

---

### 让列表更合心意（智能列）

「**设置 → 智能列设置**」里可以决定结果列表**显示哪几列**（总共 3–7 列）：

- **序号 / 文件名 / 匹配内容** 三列固定不可改；
- 中间可以加 0–4 列，填**表头名字**即可，例如 `工单任务标题`、`核定工日`；
- 填了数据里没有的列名会提示"列不存在"，换个名字就行。

---

### 设置项速查

打开「**设置**」，左侧从上到下依次是：

- **通用设置**：主题（浅色/深色）、强调色、关闭按钮的行为、是否提示说明书；
- **搜索设置**：模糊搜索开关、二级筛选模式（标准 / 逐级）；
- **搜索历史**：历史显示条数、保留时间，也可清空历史；
- **加密设置**：打开加密表格所需的附加密码；
- **共享设置**：从共享目录读取表格（见下一节）；
- **智能列设置**：结果列表显示哪些列；
- **高级设置**：屏蔽管理、标记清除、修改管理密码（需密码进入）；
- **关于我们**：版本信息、**使用说明书**、开源声明。

---

### 用共享目录（多台电脑共用一份表格）

如果你的表格放在共享文件夹里（形如 `\\服务器\共享名\子目录`）：

1. 进「**设置 → 共享设置**」；
2. 选择「**共享模式**」，填入共享目录地址，点「**测试连接**」；
3. 成功后点「**重新加载**」即可。

- 网络临时不通时，程序会**自动使用上次的结果**，不会把已有数据清空；
- 想换回本机 `data` 文件夹，选「**离线模式**」即可。

---

### 托盘与关闭

- 点标题栏的「**×**」时，默认会问你：**最小化到托盘** 还是 **直接关闭**；勾「不再询问」可记住选择。
- 想改这个行为：「**设置 → 通用设置 → 关闭选项设置**」。
- 程序最小化到托盘后：**左键单击/双击图标**恢复窗口；**右键图标 → 退出程序**才是真正退出。

---

### 升级与卸载

**升级**：直接安装新版本即可。第一次启动时，程序会**自动把旧版本的设置带过来**
（主题、屏蔽、标记、搜索历史、共享目录、密码），不需要手工重新设置。

**卸载**：运行安装目录下的 `uninstall.exe`（或从「设置 → 应用」里卸载）。卸载向导会让你选择：

- 是否删除 **`data` 里的表格文件**（默认**保留**，推荐保留）；
- 是否删除 **用户配置与历史记录**（默认**保留**）。

卸载完成后会明确告诉你哪些删了、哪些还在。

---

### 遇到问题

**打开后列表是空的**：`data` 文件夹里还没有文件，复制进去再点「重新加载」。

**提示"数据目录为空"**：同上。提示里会写明目录的完整路径，照着放即可。

**某个文件没被读进来**：可能损坏或格式特殊；加载完成后左下角会**提示哪些文件读取失败**。

**结果不是最新的**：点「**重新加载**」重新读取。平时程序会用上次的索引加快启动。

**加密表格打不开**：进「**设置 → 加密设置**」填附加密码，再重新加载。

**提示"程序已在运行"**：同一时间只允许开一个；重复打开会把已运行窗口叫到最前面。

**共享目录连不上**：检查地址与网络。不通时自动用上次结果，恢复后点「测试连接」即可。

---

### 关于本手册

- 本手册随程序分发，可在「**关于我们**」页随时打开；
- 手册**内容更新后**（例如随新版本发布），程序会在启动时**再次提示**你查看；
- 你可以在弹出的手册窗口里勾选「**下次说明书更新前不再显示**」；
- 想彻底不再提示：「**设置 → 通用设置 → 启动时不再提示说明书**」（更新后恢复提示）。

---

### 开源与致谢

- 本项目以 **GPL-3.0** 开源，完整许可见程序目录下的 `LICENSE`；
- 使用了 Qt 6（LGPL-3.0）等开源组件，声明见 `licenses` 文件夹；
- 界面视觉风格参考 MAA / MaaWpfGui 与 MaaEnd，特此致谢。
